# ToDo

- Fix the first application always launching on the workspace where the daemon was launched from
- Split the QMLs
    - Add all theme files extraction instead of only `Main.qml`
- Fix lag
- Add ability to cycle through items
- Remove `path` mode from fields
- Write about frontend APIs entry script in wiki
- Add 32 bit build support
- Close if the user tried opening again while it's already open
- Try adding Void Linux build support
